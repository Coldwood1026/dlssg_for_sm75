# dlssg_sm75 v1.0 改动说明 —— 相对原版逐字节对照

**本文回答一个问题：这个运行包相对"原版"到底动了哪些字节、为什么动。**
所有数字都是脚本实测（`tools\release_diff.py` / `tools\patch_orig_version.py` / `tools\patch_backend.py`），不是估计。

> English version: 暂无（本文暂无英文对照；用法见 `README.en.md`）。

---

## 0. 一句话结论

| | |
|---|---|
| **新增** | 1 个文件：`dinput8.dll`（自研注入载体，原版没有这个东西） |
| **修改** | 2 个文件：`version.dll` 改 **10 个字节**；`runtime\sm75_backend.dll` **重建**（换掉全部 144 个内核资源） |
| **改写** | 1 个文件：`dlssg_sm86.ini`（纯配置，无代码） |
| **零改动** | `runtime\nvngx_dlssg.dll`（原版 310.1，逐字节相同） |

没有改游戏 EXE，没有改显卡驱动，**没有改系统里的 `nvapi64.dll` 文件**（只在进程内存里改它的导出表指针，进程退出即消失）。

---

## 1. 原版基线（三份"原版"）

| 原版对象 | 出处 | 大小 (B) | SHA256 |
|---|---|---|---|
| 代理 `version.dll` | `dlssg_for_sm86-main\version.dll` | 10,522,624 | `03D445237D519AC48CD9226278A0F07AECD7AC597697697EB64404E1D51B3C5A` |
| 运行时（代理内嵌资源 id30001） | 同上，NVIDIA **310.1** | 7,596,088 | `C989C0EBD9CD21DBF3DB0BECD857AA1F218068D94DA371D924331A6F0FB97525` |
| 后端（代理内嵌资源 id30002） | 同上，作者原版 **sm86** 构建 | 2,518,016 | `DFB6D0FDEA20159A1F0D9A0CD34F25BD4FE2FBF29EC39DEE281E63276632AD89` |

> ⚠️ 蓝本目录里还有一个 `nvngx_dlssg.dll`（7,607,336 B，`907B036E…`），它和本包使用的运行时**不是同一个文件**。
> 本包用的是代理内嵌的那份 310.1（`C989C0EB…`），因为它才是 `[Backends]` 里 SHA 键所对应的运行时。

---

## 2. 改动总表

| 本包文件 | 相对原版 | 改动量 | 本包 SHA256 |
|---|---|---|---|
| `version.dll` | 蓝本代理 | **10 字节**（全在 id30002 内嵌后端里） | `2D7DC46E3B81BB29CB698A2B2EA3A4EF2DD3BCB5C90E86A47E035DF4E1EDAA37` |
| `runtime\sm75_backend.dll` | 蓝本 id30002 后端 | 2,518,016 → **2,667,520 B**；共同区间内 2,004,305 字节不同 | `D96FD76499E2EBE2926E7EA8266375E528F1F746D7F4D2B97AAA01D0C1996038` |
| `dinput8.dll` | **无原版**（新增） | 163,840 B 新代码 | `948CADAEAFC24A0CDA1AB541BBC06A4A20DA28A4835646CEA9F5701F4892A210` |
| `runtime\nvngx_dlssg.dll` | 蓝本 id30001 运行时 | **0**（逐字节相同） | `C989C0EBD9CD21DBF3DB0BECD857AA1F218068D94DA371D924331A6F0FB97525` |
| `dlssg_sm86.ini` | 蓝本 INI | 1,459 → 847 B，配置改写 | `8AC5CF8655B3E68C3B44D8039AA43DC11A5E82DAD316B67FD8A5850EDBF36DB7` |

---

## 3. 逐项详解

### 3.1 `version.dll` —— 原地改 10 个字节

代理本体（版本查询转发、加载逻辑）**一个字节都没动**。10 个字节全部落在内嵌的后端资源 id30002 里
（该 blob 在文件中的起点 = 文件偏移 `0x7A0508`，长度 2,518,016）。

| # | 文件偏移 | 后端 RVA | 原 → 新 | 语义 |
|---|---|---|---|---|
| 1 | `0x7B077A` | `0x10272` | `70` → `90` | `reported_arch` 日志字面量：`0x170` → `0x190` |
| 2 | `0x7B08D0` | `0x103C8` | `70` → `90` | `mov dword [rdi+4], 0x170` → `0x190`（覆盖调用方看到的架构字段） |
| 3 | `0x7B1912` | `0x1140A` | `70` → `90` | `GetArchInfo` 桩的返回值 `0x170` → `0x190` |
| 4 | `0x7B21BE` | `0x11CB6` | `70` → `60` | requirements 桥接转发给 NGX 核心的架构：`0x170` → **`0x160`（真实 Turing）** |
| 5 | `0x7B23F8` | `0x11EF0` | `70` → `60` | `bridge_min_arch` 日志字面量：同上 |
| 6 | `0x7B5E45` | `0x1593D` | `56` → `4B` | SM 门禁 `cmp …,0x56` → `0x4B` |
| 7 | `0x7B5E79` | `0x15971` | `56` → `4B` | 同上 |
| 8 | `0x7B5E86` | `0x1597E` | `56` → `4B` | 同上 |
| 9 | `0x7B5E9A` | `0x15992` | `56` → `4B` | 同上 |
| 10 | `0x7B6319` | `0x15E11` | `56` → `4B` | 同上（这条是 `cmp dword [rip+disp32], 0x56`） |

**为什么是这三组值：**

- **`0x56`(86) → `0x4B`(75)**：后端安装函数里的 5 处架构门禁，`0x56` 是 sm_86。放宽成 `0x4B`(sm_75) 后，
  Turing 才被认为"能装"。
- **`0x170`(Ampere/sm_86) → `0x190`(Ada/sm_90)**：游戏（巫师 3）要求 Ada 才给帧生成开关；
  原版后端如实上报 Ampere，所以开关是灰的。这三处改的是**上报给游戏的值**。
- **`0x170` → `0x160`(Turing/sm_75)**：这是 requirements 桥接**转发给 NGX 核心**的值。
  核心的判定是"真实架构 < 请求架构就拒绝"，在 Turing 卡上请求 Ampere 必然被拒；
  所以这里必须填**真实硬件架构** `0x160`，与上面"骗游戏"的 `0x190` 是两个不同的方向，不能混用。

> 这条路径（代理内嵌后端）在本包里**不生效** —— 因为 INI 用了 `Mode=Pinned` + `[Backends]`，
> 真正装载的是外部 `runtime\sm75_backend.dll`。代理里那份仍然改了，是为了"两条路径行为一致"。

### 3.2 `runtime\sm75_backend.dll` —— 重建

**基线**：`dlssg_for_sm86-main\_orig_backend_embedded.dll`（即代理 id30002 原样，2,518,016 B）

**改了三层：**

1. **换内核资源（主改动）**：`.rsrc` 里 144 个 leaf 全部重写 ——
   - 72 个 **cubin**（资源 ord `1000..1071`）← `3101_payload\slot_cubin\<2000+i>.cubin`
   - 72 个 **PTX**（资源 ord `2000..2071`）← `3101_payload\slot_rewritten\<ordinal>.ptx`，
     保留原 0x60 容器头、刷新长度字段、把架构字节 `0x2C` 写成 `0x4B`
   - 内核来源链路：310.1 内嵌的 sm_89 PTX →（`rewrite_f16` + `rewrite_mma` 降到 sm75 可用 + `.target sm_75`）
     → `ptxas -arch=sm_75 -O3` → sm75 cubin。ptxas 来自 pip 包 `nvidia-cu13`，**不是** CUDA Toolkit。
   - blob 总量 2,117,240 → 2,266,859 B（+149,619 B）
2. **叠加与 `version.dll` 同一套 10 字节补丁**（5 门禁 + 3 上报 + 2 桥接，RVA 见 §3.1 表）
3. **PE 重排**：`.rsrc` 变大 → 重算节表、`SizeOfImage`、资源目录 size，并把 `.reloc` 整体上移

**为什么共同区间里有 200 万字节"不同"**：`.rsrc` 变大导致后面所有节在文件里的位置整体平移，
逐字节比较自然大面积不等；真正的语义改动就是上面三层。

### 3.3 `dinput8.dll` —— 新增（原版没有这个文件）

它不是"改"出来的，是新写的注入载体。放在游戏 EXE 目录里，被游戏**静态导入**，所以在进程启动时必定加载。

| 做的事 | 细节 |
|---|---|
| 导出面 | 只导出真实的 6 个 dinput8 名字（`DirectInput8Create` / `DllGetClassObject` / `DllCanUnloadNow` / `DllRegisterServer` / `DllUnregisterServer` / `GetdfDIJoystick`），首次调用时转发到 `System32\dinput8.dll` |
| 改 `nvapi_QueryInterface` | 在 `nvapi64.dll` 的导出地址表里把 `nvapi_QueryInterface` 指向一段自己塞进代码洞的 `jmp qword ptr [rip+0]` 跳板（EAT 只存 32 位 RVA，只能指向模块内部） |
| `NvAPI_GPU_GetArchInfo`(`0xD8265D24`) | 对**排除名单之外**的调用方把架构强制改成 Ada `0x190`（只在版本字段为 `0x10010`/`0x20010` 且架构 < `0x190` 时改写） |
| 排除名单 | `nvngx_dlssg.dll` / `nvngx_dlss.dll` / `nvapi.dll` / `nvapi64.dll` / `nvngx_dlssg_orig.dll` —— 这些模块拿到**真实** QueryInterface（310.1 自己按架构校验，全局伪造会让安装路径打转） |
| `NvAPI_D3D12_SetRawScgPriority`(`0x5DB3048A`) | Ada-only 的调用，桩成"成功"，否则 Turing 上会摘掉设备 |
| 运行库 `GetFeatureRequirements` | 覆盖 `nvngx_dlssg.dll` 的 `NVSDK_NGX_*GetFeatureRequirements` 导出：返回成功、`Flags=0`、`RequiredGPUArchitecture=0xC0` |
| 运行库 flag 位 | NOP 掉 RVA `0x3C86E` / `0x3CAC1` / `0x3CB27` 三处 `or dword ptr [rsi], imm` |
| 日志 | 写游戏 EXE 目录下的 `dlssg_sm75_inject.log` |
| 时序 | 启动后轮询最多 120 s 等 `nvapi64.dll` / `nvngx_dlssg.dll` 出现，随后 20 s 内反复重申上述导出改写 |

**边界**：只改进程内存，磁盘上的 `nvapi64.dll`、`nvngx_dlssg.dll` 都不变；进程退出后一切还原。

> 载体名字有讲究：载体必须被目标 EXE **静态导入**才会加载。
> 巫师 3 的 `witcher3.exe` 导入 `DINPUT8.dll`，所以用这个名字；UE5 游戏（如 33 号远征队）不导入它，
> 需要换一个同样被导入的系统 DLL 名字。

### 3.4 `runtime\nvngx_dlssg.dll` —— 零改动

SHA256 `C989C0EB…` 与代理内嵌资源 id30001 **完全一致**，逐字节相同。
它只是被从代理里"取出来"单独放一份，好让 `Mode=Pinned` + `[Backends]` 能按 SHA 匹配加载。

### 3.5 `dlssg_sm86.ini` —— 配置改写

| 段 / 键 | 蓝本原版 | 本包 | 为什么 |
|---|---|---|---|
| `[FrameGeneration] MaxGeneratedFrames` | `3` | `3` | 不变（2x/3x/4x 的上限） |
| `[Logging] Level` | `2` | `3` | 排错期打开内核/评估级日志 |
| `[Compatibility] KernelImage` | `Auto` | `Cubin` | 走已验证的 cubin 路径（`Auto` 在强制路由下会退到 PTX/驱动 JIT） |
| `[Compatibility] ForceSM86Route` | `0` | `1` | 强制走"模拟 sm86 → 路由"这条路，才能进后端 |
| `[Compatibility] SimulateAmpere` | `0` | `1` | 同上 |
| `[Runtime] Mode` | `Bundled` | `Pinned` | 用外部 310.1 运行时而不是内嵌的 |
| `[Runtime] Path` | 空 | `runtime\nvngx_dlssg.dll` | 指向 §3.4 的运行时 |
| `[Backends]` | 空 | `c989c0eb…=runtime\sm75_backend.dll` | 键 = 运行时 SHA256，值 = sm75 后端 |

其余段（`[General]` 等）保持原样。

> `[Backends]` 的键**必须**等于 `runtime\nvngx_dlssg.dll` 的实际 SHA256。换运行时就要同步换这个键，
> 否则后端不会被装载。

---

## 4. 边界：明确没碰的东西

- 游戏 EXE / 游戏自身 DLL（除覆盖同名代理外）
- 显卡驱动、`System32` 下的任何文件
- NVIDIA 的 NGX 运行时本体（`runtime\nvngx_dlssg.dll` 零改动）
- 注册表

## 5. 怎么复现这些数字

```bat
python tools\release_diff.py            :: 本文所有哈希 / 差异字节 / 资源清单
python tools\patch_orig_version.py      :: 重打 version.dll（10 字节）
python tools\build_slots.py             :: 重新用 ptxas 编 72 个 sm75 cubin / PTX
python tools\patch_backend.py           :: 重打 runtime\sm75_backend.dll
```

## 6. 回滚

删掉 `version.dll`、`dinput8.dll`、`dlssg_sm86.ini`、`runtime\`、`dlssg_sm86\`、`dlssg_sm75_inject.log`
即可。`version.dll` 是原版代理文件，Steam「校验文件完整性」可以恢复游戏自身版本。

## 7. 版权

`runtime\nvngx_dlssg.dll` 与 `version.dll` 内嵌的 310.1 运行时为 NVIDIA 原版文件，版权归 NVIDIA；
本包仅供本地测试使用，请勿再分发。
