# dlssg_sm75 v1.0 —— 让 20 系（Turing / sm_75）真正跑起 DLSS 帧生成

**2026-09-09**，真机验证：RTX 2060 Max-Q + 巫师 3 次世代（DX12）。游戏内 **帧生成开关可开**，
后端安装成功（`backend_install status:0`），日志无 `bridge_error`。

> English version: `README.en.md`.
> 逐字节改动对照（相对原版改了什么、为什么）见 `改动说明.md`。

---

## 1. 这是什么

用 **原版 nvngx_dlssg 310.1** 作运行时，配一个自研的 **sm75 后端**：后端把 310.1 内嵌的
72 个内核（原本只有 sm_89 / sm_120）整体替换成用 ptxas 编出来的 **sm_75 cubin / PTX**，
再把架构门禁和上报改成 Turing 可接受的值。三个组件各司其职：

| 文件 | 角色 | 改了什么 |
|---|---|---|
| `version.dll` | 原版 `dlssg_for_sm86` 代理 | 相对原版**只改了 10 个字节**（都在内嵌后端资源里）：5× SM 门禁 `cmp …,0x56` → `0x4B`；3× 架构上报 `0x170` → `0x190`(Ada)；2× requirements 桥接 `0x170` → `0x160`(真实硬件 Turing) |
| `dinput8.dll` | 注入载体（游戏静态 import 它，必被加载） | 拦 `nvapi_QueryInterface`：给排除名单外的调用方把 `GPU_GetArchInfo` 强改 Ada `0x190`、把 Ada-only 的 `D3D12_SetRawScgPriority` 桩成成功；再把运行库的 `GetFeatureRequirements` 覆盖为「支持」（Flags=0, arch 0xC0）并 nop 3 处 flag 位 |
| `runtime\sm75_backend.dll` | sm75 后端（重建） | 144 个资源 = 72× sm_75 cubin + 72× sm_75 PTX；门禁/上报/桥接与 `version.dll` 同套补丁 |
| `runtime\nvngx_dlssg.dll` | 运行时 | **未改动**，原版 310.1 |

> `version.dll` 与 `runtime\sm75_backend.dll` 是两条独立路径，补丁内容一致。
> 本包用 **Pinned + `[Backends]`** 走外部后端（见 INI），所以真正生效的是 `runtime\sm75_backend.dll`。

---

## 2. 包内容（SHA256）

| 路径 | 字节 | SHA256 |
|---|---|---|
| `version.dll` | 10522624 | `2D7DC46E3B81BB29CB698A2B2EA3A4EF2DD3BCB5C90E86A47E035DF4E1EDAA37` |
| `dinput8.dll` | 163840 | `948CADAEAFC24A0CDA1AB541BBC06A4A20DA28A4835646CEA9F5701F4892A210` |
| `dlssg_sm86.ini` | 847 | `8AC5CF8655B3E68C3B44D8039AA43DC11A5E82DAD316B67FD8A5850EDBF36DB7` |
| `runtime\nvngx_dlssg.dll` | 7596088 | `C989C0EBD9CD21DBF3DB0BECD857AA1F218068D94DA371D924331A6F0FB97525` |
| `runtime\sm75_backend.dll` | 2667520 | `D96FD76499E2EBE2926E7EA8266375E528F1F746D7F4D2B97AAA01D0C1996038` |

> `[Backends]` 的键 **必须**等于 `runtime\nvngx_dlssg.dll` 的实际 SHA256（即上表 `c989c0eb…`），
> 换运行时就要同步换这个键，否则后端不会被装载。
>
> 包内 `dlssg_sm86.ini` 是**真机工作版原样**（逐字节，含注释）。它 `[Compatibility]` 段上方的注释
> “Auto on a forced route -> PTX … Try Cubin later” 已经过时：实际生效值是 `KernelImage=Cubin`，
> 别照注释去改。

---

## 3. 安装

1. 关掉游戏。
2. 把本目录的 `version.dll`、`dinput8.dll`、`dlssg_sm86.ini` 和整个 `runtime\` 目录
   复制到**实际渲染 EXE 所在目录**（巫师 3 次世代：`...\The Witcher 3\bin\x64_dx12\`），覆盖同名文件。
3. 删掉旧缓存：清空 `%LOCALAPPDATA%\DlssgSm86\bundles\`。
   （旧包留下的后端 bundle 会干扰 SHA 匹配，必须清。）
4. 游戏目录里原有的 `nvngx_dlssg.dll` 不用动 —— 加载请求会被 `version.dll` 重定向到
   `runtime\nvngx_dlssg.dll`。若之前装过别的整合包（如 FSR3 版）替换过它，也保持现状即可。
5. 启动游戏 → 显示/图形设置里打开 **DLSS 帧生成**。

要求：Windows x64 + 支持 DLSS 3 的驱动 + RTX 20 系（Turing）显卡。

---

## 4. 验证它真的在工作

**`dlssg_sm86\logs\loader_<pid>.jsonl`**
```
"event":"configuration"   → "runtime_mode":"pinned", "kernel_image_requested":"cubin"
"event":"backend_install" → "status":0, "backend":"...\runtime\sm75_backend.dll"
```

**`dlssg_sm86\logs\backend_<pid>.jsonl`**
```
"event":"mfg_capability" → "changed":true, "reported_max":3      ← 多帧生成能力被接受
```
**不应出现** `bridge_error` 或 `Embedded SM86 kernel missing`。

**`dlssg_sm75_inject.log`**（同 EXE 目录）
```
[inject] GetArchInfo arch 0x160 -> 0x190 (Ada)
[inject] GetFeatureRequirements -> success (flags 0, arch 0xC0)
[inject] QI(D3D12_SetRawScgPriority) -> stub
```

游戏内：帧生成开关能打开，帧数明显上升。

---

## 5. 已知情况 / 限制

- backend 日志里可能出现一条
  `{"event":"kernel_create", "source_rva":"0xda7a0000", "routed":false, "status":-14}`
  —— 那是 nvngx 自身的一次空 blob 探测调用（blob=NULL），后端不参与，**无害**。
- 最大生成帧数 3（`MaxGeneratedFrames=3`）。
- 只用巫师 3 DX12 验证过；其它支持 DLSS-FG 的游戏理论上通用，但未测试。
- `KernelImage=Cubin` 是已验证路径；改成 `Auto` 会退回 PTX（驱动 JIT），仅作排错用。

---

## 6. 卸载

删掉 `version.dll`、`dinput8.dll`、`dlssg_sm86.ini`、`runtime\`、`dlssg_sm86\`、`dlssg_sm75_inject.log`。
`version.dll` 是原版代理文件，Steam「校验文件完整性」可恢复游戏自身版本。

---

## 7. 来源

- 蓝本：`dlssg_for_sm86-main`（原版 `version.dll` 代理 + 后端资源）。
- 架构伪装思路参考：`dlssg-to-fsr3`（nvapi `GPU_GetArchInfo` 返回 Ada）。
- `nvngx_dlssg.dll` 为 NVIDIA 原版运行时文件，版权归 NVIDIA；本包仅供本地测试使用。
